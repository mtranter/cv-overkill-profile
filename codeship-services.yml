gulp:
  build:
    dockerfile: Dockerfile.build.ui
  cached: true
terraform:
  build:
    dockerfile: Dockerfile.deploy
  volumes:
    - ./tmp/app:/app
  encrypted_env_file: env.encrypted
  cached: true
